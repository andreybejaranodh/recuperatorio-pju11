const db = require('../../database/models');

const controller = {
    async index(req, res) {
        // Crear controlador para retornar todas las categorias
    },
    async create (req, res) {
        // Crear controllador para crear un nueva categoria
    }
}

module.exports = controller;